
import static org.junit.Assert.*;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;

/**
 * The test class ProgessorTest.
 *
 * @author  (your name)
 * @version (a version number or a date)
 */
public class ProfessorTest extends junit.framework.TestCase
{
    /**
     * Default constructor for test class ProgessorTest
     */
    Professor pro1;
    String namef1="a";
    String namel1="b";    
    public ProfessorTest()
    {
        pro1=new Professor(namef1,namel1);
    }

    /**
     * Sets up the test fixture.
     *
     * Called before every test case method.
     */
    @Before
    public void setUp()
    {
    }

    /**
     * Tears down the test fixture.
     *
     * Called after every test case method.
     */
    @After
    public void tearDown()
    {
    }
    
    public void testSetOffice(){ 
        pro1.setOffice("room1");
        assertEquals("room1",pro1.getOffice());
    }
}
